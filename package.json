{
  "name": "goatool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "goatool browser tools for Korean public submission, job application files, PDFs, photo resizing, and cleanup.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/postbuild.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^7.1.12"
  }
}
